.vscode/**
src/**
esbuild.js
tsconfig.json
**/*.map
.gitignore
.agents/**
docs/**
scripts/**
.playwright-cli/**
# cheerio is bundled into dist by esbuild; playwright-core must stay external
# (it spawns browsers at runtime), so it is the only dependency shipped whole.
node_modules/**
!node_modules/playwright-core/**
